hello
setwd("~/GitHub/Summer-Project")
setwd("~/GitHub/Summer-Project")
setwd("~/GitHub/Summer-Project")
#parameters
maxage = 4 #it is max age 3 plus one because they die after age 3 and after they reproduced #anything that makes it more generalizeable can be put as a parameter
k = 100 #population size which will fluctuate , around 100 with variance
broodsize= 1 #the max amount of offsprings per pair
poptime= 50 #we want the population to exist for 50 years
#initialize population
pop = matrix(nrow=100, ncol= 4)
colnames(pop) <- c("id", "sex", "mom", "dad")
pop[,1] = seq(1,100,1)                    #each individual has unique ID name
pop[,2] = sample(c(0,1),100,replace=T)    #each individual assigned male (1) or female (0), replace = T because if it draws 1 it can draw a 1 and a 0 the next time
pop[,3] = rep(0,100)                      #mom id - later will not be 0, this is useful for debugging
pop[,4] = rep(0,100)                      #dad id - later will not be 0, this is useful for debugging
View(pop)
?sample
setwd("~/GitHub/Summer-Project")
#parameters
maxage = 4 #it is max age 3 plus one because they die after age 3 and after they reproduced #anything that makes it more generalizeable can be put as a parameter
k = 100 #population size which will fluctuate , around 100 with variance
broodsize= 1 #the max amount of offsprings per pair
poptime= 50 #we want the population to exist for 50 years
#initialize population
pop = matrix(nrow=100, ncol= 5)
colnames(pop) <- c("id", "sex", "mom", "dad", "age")
pop[,1] = seq(1,100,1)                    #each individual has unique ID name
pop[,2] = sample(c(0,1),100,replace=T)    #each individual assigned male (1) or female (0), replace = T because if it draws 1 it can draw a 1 and a 0 the next time
pop[,3] = rep(0,100)                      #mom id - later will not be 0, this is useful for debugging
pop[,4] = rep(0,100)                      #dad id - later will not be 0, this is useful for debugging
pop[,5] = sample(c(-1,0,1,2,3),100,replace=T) #sample is the function and a vector of the ages -1-3 because minus one age
table(pop[,5])
